server:
  port: 8010
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: authentication-api
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      javax:
        persistence:
          validation:
            mode: none
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
  datasource:
    hikari:
      maximum-pool-size: 10
      connection-timeout: 10000
      minimum-idle: 2
      idle-timeout: 10000
    url: ${DB_URL:jdbc:postgresql://testdb.c123lyxq7he8.eu-central-1.rds.amazonaws.com:5432/prod}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:2425848000a.}

jwt.secret: suleymankotan
